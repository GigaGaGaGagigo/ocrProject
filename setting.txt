# 가상환경 (선택)
python -m venv venv       # easyocr, paddleocr 구동을 위해선 3.9ver설치 환경 따로 필요 
source venv/bin/activate  # (윈도우: venv\Scripts\activate)

# 설치
pip install -r requirements.txt
pip install Pillow
pip install pytesseract gradio Pillow
pip install streamlit pytesseract pillow

# easy ocr 설치
pip install easyocr streamlit Pillow

# paddle ocr 설치
pip install paddlepaddle==2.5.1 # 일반적으로 
pip install paddlepaddle==2.5.1 -f https://www.paddlepaddle.org.cn/whl/macos/mps.html # 맥의 경우 권장
pip install paddleocr==2.6.1.3 --no-deps 
pip install pandas tqdm opencv-python matplotlib scikit-learn
pip install imgaug
pip install numpy==1.24.3
pip install lmdb

# 추가 설치
pip install streamlit sqlalchemy pymysql # DB연동을 위함 

.\venv\Scripts\python.exe -m pip install selenium

# 실행
python app.py

# streamlit 실행
streamlit run streamlit_webtoon_ocr.py
streamlit run ui_test2.py
streamlit run main.py
streamlit run db_test.py
streamlit run test/ui/test_easyocr_page.py